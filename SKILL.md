---
name: qc-radar-single
description: 为医院质量控制数据按科室生成独立 Word 报表（含雷达图）并打包为 ZIP。读取质控数据 CSV，在全院核心临床科室间计算各指标排名与 ABCD 评级，逐科室生成一个 Word 文档，内含指标-数值-评级表格和"能力及效率/医疗质量与安全"两张雷达图，最后打包 ZIP 发送。使用场景：医院质量管理、医务/质控科月报、按科室分发的质控报表。
version: 1.0.0
author: Deep
license: MIT
---

# qc-radar-single — 分科室质量控制报表生成器

## 功能

读取医院质控数据，为每个核心临床科室生成一个独立的 Word 质量控制报表并打包为 ZIP：

- **排名口径**：全院核心临床科室（有"出院人次"的科室）参与排名
- **评级**：按排名四档 A（前25%绿）/ B（25-50%蓝）/ C（50-75%橙）/ D（后25%红）
- **达标类指标**：DDDs（直接读取 CSV `DDDs达标` 列）、医疗不良事件上报数 → 达标/不达标
- **不排名指标**：死亡例数（住院）仅显示数值，不参与排名
- **雷达图**：每个科室两张雷达图（能力及效率 + 医疗质量与安全），中文使用 macOS 自带 STHeiti 字体
- **输出**：每个科室一个 `.docx` + 打包为 `.zip`

## 使用方式

### 数据准备（必须）
1. 质控数据 CSV：含列 `科室` 及指标列，如 `出院人次,门（急）诊人次,医疗服务收入占比,...,DDDs,DDDs达标,(48小时)再入院例数,...,医疗不良事件上报数,督查扣分,临床路径入径率`。必须包含 `DDDs达标` 列（值为"达标"/"不达标"）。数值空为"-"或空。
2. DDD 目标值 CSV：列 `科室,DDD目标值`。科室名会自动做规范化匹配（去除括号及空格）。**仅供参考，DDDs 达标判定以质控数据 CSV 的 `DDDs达标` 列为准**。

### 数据放置（推荐）
脚本默认在**脚本所在目录的 `data/` 子目录**中读取数据并输出结果，路径全部相对脚本位置，方便随技能整体携带：

```
qc-radar-single/
  gen_qc_reports.py
  SKILL.md
  data/
    质控数据提取结果.csv
    ddds目标值.csv
    科室单报表/        ← 输出
    科室质控报表.zip   ← 输出
```

### 数据放置（推荐）
脚本默认在**脚本所在目录的 `data/` 子目录**中读取数据并输出结果，路径全部相对脚本位置，方便随技能整体携带：

```
qc-radar-single/
  gen_qc_reports.py
  SKILL.md
  data/
    质控数据提取结果.csv
    ddds目标值.csv
    科室单报表/        ← 输出
    科室质控报表.zip   ← 输出
```

### 运行

直接运行（使用 `data/` 默认路径）：
```bash
uv run python skills/qc-radar-single/gen_qc_reports.py
```

或自定义路径：
```bash
uv run python skills/qc-radar-single/gen_qc_reports.py \
  --data "Z:\...\质控数据提取结果.csv" \
  --prev "Z:\...\上月质控数据提取结果.csv" \
  --ddds "Z:\...\ddds目标值.csv" \
  --out "Z:\...\科室单报表" \
  --zip "Z:\...\科室质控报表_7月.zip"
```

> `--prev` 为可选参数，传入上月 CSV 后表格将新增「上月」「增减」两列，自动计算环比变化百分比，绿色表示向好，红色表示向差。

### 依赖
- python-docx、matplotlib、numpy（uv 环境）
- macOS 字体：系统自带 STHeiti (华文黑体)，无需额外安装

## 关键逻辑

### 指标分类
- `GROUPS`：能力及效率（13项）/ 医疗质量与安全（13项）
- `POSITIVE`：越大越好（出院人次、CMI值、床位使用率、手术占比、检验危急值处理率、临床路径入径率等）
- `NEGATIVE`：越小越好（次均费用、平均住院日、死亡例数、再入院、督查扣分等）
- `COUNT_METRICS`：整数计数显示
- `PERCENT_METRICS`：百分比显示
- `SPECIAL_METRICS`：DDDs（读取 CSV `DDDs达标` 列）、医疗不良事件上报数 → 达标判定
- `NO_RANK_METRICS`：死亡例数（住院）→ 不排名，仅显示数值

### 特殊规则
- 临床路径入径率 >50% → 强制 A，≤50% → 强制 D
- 检验危急值处理率 =100% → 强制 A

## 目录
- `gen_qc_reports.py` — 主生成脚本
- `SKILL.md` — 本说明